# MSU IR — Institutional Repository (Mahasarakham University)

Laravel 12 + Inertia.js v2 + Vue 3 (TypeScript) + Tailwind CSS. XAMPP/MySQL locally, DB name `msuir_db`.

## Conventions established so far

- **Admin backend is one file**: `resources/js/pages/Dashboard.vue` — sidebar + all tabs (dashboard/repository/approvals/analytics/members/settings) live in this single SFC via `activeTab` ref, not separate routed pages. No shared `AppSidebarLayout` is used here (that layout exists in `resources/js/layouts/app/` but is currently disconnected/unused).
- **Public pages** use `resources/js/layouts/PublicLayout.vue` (Welcome, Collection, Item, Tutorial) — wraps `PublicNavbar`, `LoginModal`, `CookieBanner`, `ImpersonationBanner`.
- **"Collection" is the canonical name** for the browsable subject grouping (e.g. "MSU e-Theses"). Route `/collection/{id}` → `CollectionController@show` → renders `Collection.vue` with prop `collection`. Do NOT reintroduce the word "category" anywhere (renamed 2026-09-01). `book_type` (material type: งานวิจัย / คู่มือ / e-book) is a **separate independent dimension** from `collection`.
- **Inertia action pattern**: admin mutations (create/update/delete/toggle) call `router.post/put/patch/delete(route('...'), payload, { preserveScroll: true, onSuccess, onError })` and the controller just `return back()->with(...)` — no client-side state mutation, Inertia re-fetches props automatically. Follow this pattern for any new admin CRUD.
- **Confirm-before-destructive-action**: use `Swal.fire({...})` (sweetalert2) for delete/impersonate/logout confirmations, not `confirm()`. See `handleDeleteMember`/`handleImpersonate`/`logout` in Dashboard.vue for the exact options object to reuse.
- **Toast**: `triggerToast(message, 'success'|'warning'|'danger')` — local component pattern, not a global store.
- **Styling language**: `rounded-2xl`/`rounded-[2.5rem]` cards, `bg-[#1e3a8a]` primary / `#facc15` yellow accent, Font Awesome 6 icons (loaded via CDN `<link>` in each page's `<Head>`), Sarabun font. Modals: gradient header (`from-[#1e3a8a] via-[#1e40af] to-blue-700`) + white form body, `modal-fade` transition.
- **Custom dropdowns**: native `<select>` open-list can't be restyled cross-browser. Use `resources/js/components/ModernSelect.vue` (self-built, `@vueuse/core` `onClickOutside`) instead of `<select>` anywhere inside this admin UI's modals.
- **Modal card gotcha**: the outer modal card must NOT have `overflow-hidden` on the whole card if it contains a `ModernSelect` (the dropdown list gets clipped). Put `overflow-hidden rounded-t-[2.5rem]` on the gradient header div only (to clip its decorative blur circle), keep the outer card overflow visible.

## Data model built so far

**`users` table** (extended from the starter kit's default):
- `role_level` (tinyint, pre-existing): 1 = สมาชิกทั่วไป, 3 = ผู้ดูแลระบบ (level 2/staff exists in DB but UI only exposes 1 and 3)
- `status`: `'active' | 'suspended'`, toggled by clicking the status badge in the members table (not a separate icon anymore)
- `is_msu_member` (boolean): true = มมส. (@msu.ac.th, logs in via Google OAuth, no password), false = บุคคลภายนอก (full email + password)
- `department_id` (nullable FK → `deps.id`, `nullOnDelete`)
- Soft deletes enabled (`deleted_at`)

**`deps` table**: `id, name` — 49 rows seeded via `database/seeders/DepsSeeder.php` (real MSU faculty/office list, confirmed by user). Reseed with `php artisan db:seed --class=DepsSeeder`.

**Admin member management** (`resources/js/pages/Dashboard.vue`, tab `members`):
- Real DB-backed (no more mock), props `members` + `departments` come from `DashboardController@index`
- Routes (all under `role:3` middleware, alias registered in `bootstrap/app.php`):
  - `POST admin/members` → `MemberController@store`
  - `PUT admin/members/{member}` → `MemberController@update`
  - `PATCH admin/members/{member}/status` → `toggleStatus`
  - `DELETE admin/members/{member}` → `destroy` (soft delete)
  - `POST admin/members/{member}/impersonate` → `impersonate` (real session-swap, blocked for self/other-admins/suspended accounts)
  - `POST impersonate/leave` (auth-only, not role:3) → `ImpersonateController@leave`
- Impersonation state shared via `auth.impersonating` (Inertia shared prop, `HandleInertiaRequests`), banner in `ImpersonationBanner.vue`
- "เพิ่มสมาชิก"/"แก้ไขข้อมูลสมาชิก" modals: MSU-member toggle up top decides email shape (`local@msu.ac.th` compound input, no password field) vs external (full email + password required on create, optional-to-change on edit)

## In progress / next up — Items & Categories schema (research content import)

**Not yet implemented** — migrations not written yet. Design is LOCKED as of 2026-09-01 (round 6). The real CSV has been re-exported with the final `collection_id` column.

**Real data** (`Element ข้อมูล MSUIR.csv`, 279 rows). Flat 1-row-per-item export, much simpler than the ubru.ac.th sample pages (structural reference only, NOT MSU content — confirmed). **Single header row** (data starts row 2), 29 columns:
`collection_id, title, alternative1, alternative2, creator, contributor ×8, subject ×5, publicsher (sic), date, format, identifier, language, right, description, degree, owner, update_at, created_at`
- The `contributor` and `subject` headers **repeat verbatim** (not `contributor1..8`) → `msuir:import` maps them positionally (any header matching `/^contributor/` or `/^subject/` becomes a list). Fields containing commas are properly `"`-quoted, so `fgetcsv` handles them.
- `collection_id` = **numeric FK → `collections.id`** (replaces the old `book_type` string). Observed values in the CSV: `2` (research, the majority), `4` (e-books), `5` (manuals) — consistent with the id table below.
- `creator` = single main author (name only, no org/email). `contributor1..8` = co-authors (name only). Some rows have no author at all → nullable.
- `subject1..5` = free-text ThaSH-style headings with ` --` subdivisions; **one cell may pack several headings comma-separated** → split needed. No `keyword:`/`ThaSH:`/`DDC:` scheme prefixes.
- `publicsher` = mostly "มหาวิทยาลัยมหาสารคาม"; when it's a faculty/office name it matches the existing **`deps`** table → map to `department_id`, no separate `publishers` table.
- `date` = **stored as-is (พ.ศ. for Thai rows 2541–2569, ค.ศ. for the ~13 English rows 2016–2023)** — keep the source value, do NOT convert.
- `description` = single nullable free-text (Thai grant-acknowledgement line on Thai rows, English on English rows, frequently empty). NOT a th/en pair.
- `identifier` = one fulltext PDF URL per item → single `fulltext_url` column, no rich `item_files` table.
- `degree`, `owner`, `update_at`, `created_at` columns are **empty in every row**.
- Encoding: the attached CSV is still UTF-8-mis-read-as-Latin-1 (mojibake). Import needs a clean **CSV UTF-8** re-save. Some `identifier` URLs contain spaces (need encoding). Near-duplicate rows exist (e.g. 71/72, 132/133, 251–253 by old ID).

**`collection` and `book_type` are ONE axis** (round 6 decision — merged). No `book_types` table, no `book_id`. `categories` (3 fixed groups) → `collections` (13 leaves, `category_id` FK) → `items` (`collection_id` FK, NOT NULL). The 3-category grouping is only for the PublicNavbar dropdown; the browsable detail page is `/collection/{id}` only (no `/category/{id}`). The "ประเภททรัพยากร" filter previously scaffolded in `Collection.vue` is now redundant and should be REMOVED (you're already inside one leaf collection) — keep the sidebar filters: search-within / year / faculty.

**Seed data** — `categories`:
| id | name_en |
|----|---------|
| 1 | INSTITUTIONAL REPOSITORY (MSU-IR) |
| 2 | ARCHIVE AND RARE BOOKS |
| 3 | MULTIMEDIA & E-LEARNING |

`collections` (global id space; `name_th` = the old book_type label where one exists, else null):
| id | name_en | name_th | category_id |
|----|---------|---------|-------------|
| 1 | MSU e-Theses | วิทยานิพนธ์ | 1 |
| 2 | MSU e-Researches | งานวิจัย | 1 |
| 3 | MSU e-Articles | บทความ | 1 |
| 4 | MSU e-Books | หนังสืออิเล็กทรอนิกส์ | 1 |
| 5 | MSU e-Manual | คู่มือ | 1 |
| 6 | Local Wisdom Collection | – | 2 |
| 7 | Rare Books | – | 2 |
| 8 | Manuscripts | – | 2 |
| 9 | Historical Photographs | – | 2 |
| 10 | MSU Multimedia Archives | – | 3 |
| 11 | E-Lecture Series | – | 3 |
| 12 | Research Reports | – | 3 |
| 13 | Free e-Books | – | 3 |

**Finalized schema:**
```
categories    id, name_en, slug, sort_order, timestamps
collections   id, category_id FK→categories, name_en, name_th nullable, slug, sort_order, timestamps
items         id, collection_id FK→collections (NOT NULL), department_id FK→deps nullable (from publicsher),
              owner_id FK→users nullable, title, description nullable,
              year_issued smallint (keep source value, พ.ศ./ค.ศ. mixed), language enum('tha','eng'),
              rights nullable, format default 'pdf', degree nullable (empty now, kept for future),
              fulltext_url, status enum(pending/approved/action_required) default 'approved',
              timestamps + softDeletes
item_titles   id, item_id FK, title, sort_order   (from alternative1/alternative2)
item_person   id, item_id FK→items (cascade), name varchar, role enum(creator/contributor), sort_order,
              index(name), unique(item_id,name,role)   — author NAME stored inline; NO people table
item_subject  id, item_id FK→items (cascade), value varchar, sort_order,
              index(value), unique(item_id,value)      — subject heading stored inline; NO subjects table
```
`item_person` / `item_subject` are plain hasMany child tables (not junctions). `Item::people()` / `creators()` / `contributors()` (filter on `role`) / `subjects()` all `hasMany(...)->orderBy('sort_order')`, via models `App\Models\ItemPerson` / `ItemSubject` (`$table` set explicitly). Author-browse / subject-browse = `SELECT DISTINCT` on those columns (no canonical list; every spelling variant is its own "author"). **User decided 2026-09-01 (round 8): drop the `people` / `subjects` lookup tables — CSV carries only names, no enrichment data, so a lookup table earns nothing here.**

Dropped (no supporting data): `book_types`, `publishers`, `people`, `subjects`, rich `item_files`, org/email/contributor_role on people, `scheme` on subjects, th/en description split, 3-date model, `grantor`.

**Status (2026-09-01) — DB BUILT & DATA IMPORTED (schema round 8: no people/subjects tables).** migrations (`2026_09_01_000001,000002,000005,000006,000007,000008` — note 000003/000004 for people/subjects were DELETED), models (`Category` `Collection` `Item` `ItemTitle` `ItemPerson` `ItemSubject`), `CollectionSeeder`, and `php artisan msuir:import {path} [--fresh]` are written, lint-clean, **and have been run against `msuir_db`**:
- `php artisan migrate` → 6 tables created
- `php artisan db:seed --class=CollectionSeeder` → 3 categories + 13 collections (ids 1–13 pinned, Thai names on the 5 MSU-IR ones)
- `php artisan msuir:import "database/data/elementmsuir.csv" --fresh` → **279 items, 0 skipped** · item_titles 172 · item_person 559 (name inline) · item_subject 919 (value inline). Collection spread: `2`=269, `4`=1, `5`=9. Year range 2010–2569 (พ.ศ./ค.ศ. mixed, kept as-is). 13 eng items. 2 items have no people (CSV rows with blank creator+contributors — allowed).
- The clean source CSV lives at `database/data/elementmsuir.csv` (UTF-8 + BOM; importer skips the BOM). Filename is `elementmsuir.csv`, NOT "Element ข้อมูล MSUIR.csv".

Importer behaviour: header row found by first cell `collection_id`; `contributor`/`subject` columns mapped positionally (`/^contributor/`, `/^subject/`); `date` → digits only, kept as-is; author names + subject headings inserted verbatim as child rows (no lookup, no `firstOrCreate`), deduped only by (name, role) / (value) **within one CSV row**; creator gets `sort_order` 0 then contributors 1,2,3… in file order; whole run in one transaction. Re-run cleanly with `--fresh` (truncates items / item_titles / item_person / item_subject).

**`department_id` matcher — DONE.** `ImportMsuir::$resolveDept` closure: exact `deps.name` match → else strip the trailing `" (สำนักงานอธิการบดี) มหาวิทยาลัยมหาสารคาม[,]"` suffix and take the longest `deps.name` that is a substring of what's left (so `"ภาควิชาX คณะY …"` → `คณะY`, `"งานทันตกรรม รพ.สุทธาเวช คณะแพทยศาสตร์ …"` → `คณะแพทยศาสตร์`). After re-import: **25/279 items** carry a `department_id` (18 distinct units); the 1 remaining faculty-ish publisher, bare `"สำนักงานอธิการบดี มหาวิทยาลัยมหาสารคาม"`, has no `deps` row so stays null (correct); the other ~253 are bare `"มหาวิทยาลัยมหาสารคาม"` → null.

**`book_type` filter — REMOVED.** `selectedBookTypes` / `toggleBookType` / `availableBookTypes` prop / `isBookTypeOpen` / the "ประเภททรัพยากร" collapsible block + its chips are all gone from `Collection.vue`; `availableBookTypes` dropped from `CollectionController@show`'s payload. Build passes. Sidebar filters left: search-within (`searchQuery`) / year (`selectedYears`) / faculty (`selectedFaculties`).

**`/collection/{id}` → real data — DONE (2026-09-02).**
- `CollectionController@show(Request, int $id)`: `Collection::with('category')->findOrFail($id)`; reads `q` / `years[]` / `faculties[]` / `sort` (`date`|`title`) off the query string; `$collection->items()` eager-loads `titles` (limit 1) / `creators` / `department`; search-within = `title LIKE` OR `whereHas('people', name LIKE)` OR `whereHas('titles', title LIKE)`; `whereIn('year_issued', …)`; `whereHas('department', whereIn('name', …))`; sort `title` → `orderBy('title')`, else `orderByDesc('year_issued')->orderByDesc('id')`; `paginate(20)->withQueryString()`.
- Facet options come from the **whole collection** (not the filtered set): `availableYears` = distinct non-null `year_issued` desc; `availableFaculties` = `Dept::whereIn('id', <distinct department_id>)` names.
- Payload: `collection {id,name,name_en,category,description:null,icon:'fa-book'}` · `items[] {id,title,title_en,author,faculty,year,language,abstract}` (author = `creators.pluck('name').implode(', ')` or null) · `pagination {total,currentPage,lastPage,perPage}` · `filters {q,years,faculties,sort}` · `availableYears` · `availableFaculties`.
- `Collection.vue` `<script setup>` fully rewritten: props all-required (no `withDefaults`/mock), state seeded from `props.filters`, `applyFilters(extra)` → `router.get(route('collection.show', props.collection.id), {q,years,faculties,sort,...extra}, {preserveState,preserveScroll,replace})`, debounced search watch (350ms), `sortBy` watch, `toggleYear`/`toggleFaculty`/`clearFilters` re-query, `goToPage` + `pageNumbers` (windowed 1…last). Template: dropped `typeStyle`/`item.type` badge (now a ไทย/ENG language chip), `advisor`, downloads/views footer, `Math.random()` year counts, hardcoded "อัปเดตล่าสุด พฤษภาคม 2568"; added empty-state card; pagination wired to `pagination.*` + `goToPage`. Smoke-tested against `msuir_db` (collection 2: 269 items, 14 pages, facets + combined q/year/sort filter all correct). Build passes (Collection.js 25.4→22.6 kB).
- `routes/web.php`: `Route::get('/item/{id}', [ItemController::class, 'show'])->name('item.show')` already exists (list/grid cards `<Link :href="route('item.show', item.id)">`). `ItemController@show` itself is still whatever it was — the single-item page is NOT part of this task.

## Repository management (Dashboard) — LOCKED design, 2026-09-02, build in progress

**Roles.** `role_level` 1 = สมาชิกทั่วไป, 2 = staff, 3 = admin. `CheckRole` alias `role` (`bootstrap/app.php`) checks `role_level >= {level}`.
- `/dashboard` route = `['auth','verified','role:2']` (staff + admin; role 1 → 403). **Was briefly `role:3`, reverted 2026-09-02.**
- Inside the dashboard, **admin-only tabs** = `members` + `import` (CSV). Everything else (dashboard/repository/contribute/approvals/analytics) is staff-visible.
- `Dashboard.vue`: `isAdmin = computed(() => role_level >= 3)` (reads `page.props.auth.user.role_level`); `v-if="isAdmin"` on the members + import nav buttons (desktop **and** mobile); `watch(activeTab, …)` forces `activeTab` back to `'dashboard'` if a non-admin lands on `adminOnlyTabs = ['members','import']`.
- `DashboardController@index(Request)`: loads `members` + `departments` **only when `role_level >= 3`**, else passes `[]` — staff never receives member PII. Member mutation routes + all `import/*` routes stay/are `role:3`.
- **"การตั้งค่าระบบ" (settings) tab — DELETED** entirely (nav desktop + mobile, profile-dropdown entry, header title line, whole tab body).

**Two ingest paths:**

**Flow A — นำเข้าข้อมูล (.csv)** · admin-only tab `import` · imported items get `status = 'approved'` (live immediately).
- Step 1: click a file-icon card → downloads `elementmsuir-template.csv` (29-col header + 1–2 example rows). Admin edits offline, comes back, uploads. Accept `.csv` only, ~5 MB.
- Step 2 (dry-run, no writes): `POST admin/repository/import/validate` (multipart) stores the upload at `storage/app/imports/{uuid}.csv`, parses via the shared service, returns JSON: header check (missing/unexpected cols); per-row — empty `title` → error, `collection_id` not in `collections` → error, `date` has no digits → warning, resolved `publicsher` → which dept, **title already in DB (same `collection_id` + same `title`) → warning "พบข้อมูลซ้ำ — จะข้ามแถวนี้"**; summary counts (total / ready / duplicate-skip / error-skip) + ~20-row preview. Returns the `uuid`.
- Step 3: `POST admin/repository/import/commit {uuid}` → real insert in a transaction (**append**, not `--fresh`); skips error rows + duplicate rows; rest `status='approved'`; deletes the temp file; returns created/skipped summary + link into the repository table.
- **Impl:** extract row-parsing out of `App\Console\Commands\ImportMsuir` into `App\Services\MsuirCsvImporter` (parse / validate / commit). The Artisan command calls the service; the new `RepositoryImportController` calls the same service. `--fresh` stays CLI-only.

**Flow B — เพิ่มรายการ** · new dashboard tab `contribute` · `role:2` (staff + admin) · staff-created → `status='pending'`, admin-created → `status='approved'`. Sets `owner_id = auth()->id()`. 5-step wizard in the main area (not a modal, not a standalone route):
1. `collection_id` (ModernSelect grouped by category) · `title` · alt titles `item_titles[]` (repeatable) · `language` (tha/eng)
2. `creator` (one field) · `contributors[]` (repeatable, order = `sort_order`)
3. `year_issued` · `department_id` (ModernSelect from `deps`) · `rights` · `degree` · `description`
4. `subjects[]` (repeatable)
5. radio แนบลิงก์ / อัปโหลด PDF · review panel · save
- Save = one nested `POST admin/repository/items` → controller wraps `Item::create` + `titles()/people()/subjects()->createMany()` in a transaction (Inertia `router.post` + `back()->with()` pattern).

**Flow C — repository table → real data** (the `repository` tab list, currently mock). Role-aware `RepositoryController@index`:
- staff → `Item::where('owner_id', me)` only; admin → all items.
- `with('collection','creators','department')` + search (id / title / creator) + filter collection + filter status + paginate. Columns: ID · collection · title+creator · year · status badge (approved/pending/action_required) · actions. **Drop the วิว/โหลด columns** (no such data in schema).
- Row actions: **edit** — staff only on own item while `status IN (pending, action_required)`; admin any. **delete** — admin only (soft delete, Swal confirm).
- Tab header buttons: "นำเข้า CSV" (→ Flow A, admin only) + "เพิ่มรายการ" (→ Flow B).

**Flow D — edit item = single flat page** (decision: not the wizard). `GET admin/repository/items/{item}/edit` → `Repository/ItemEdit.vue`, every field + child lists (titles/people/subjects as repeatable rows) on one scrolling page. `PUT admin/repository/items/{item}` → update the item, delete existing child rows, `createMany` the new set (simpler than diffing), one transaction.

**Flow E — คิวตรวจสอบข้อมูล** (the `approvals` tab, currently mock → real). Role-aware:
- staff → `Item::where('owner_id', me)`, **all** statuses, **read-only**; shows status badge, the admin's note when `action_required`, and an "แก้ไข" link into Flow D/B. No approve button.
- admin → all items `status IN (pending, action_required)`; **✓ อนุมัติ** (→ `approved`) and **⚠ ส่งกลับแก้ไข** (→ `action_required` + note).
- Needs an `action_required` note column on `items` (add in the migration step) — or a small `item_reviews` table (decide when building Flow E).

**Schema change (Flow B step 5, decision "link OR file"):** migration `add_fulltext_path_to_items_table` — keep `fulltext_url` (nullable, pasted link), add `fulltext_path` (nullable string, uploaded PDF at `storage/app/public/fulltext/`). `Item::getFulltextAttribute()` → `fulltext_url ?: (fulltext_path ? Storage::disk('public')->url(fulltext_path) : null)`. Run `php artisan storage:link` once. CSV import only ever sets `fulltext_url` (from `identifier`).

**Header "เพิ่มผลงานใหม่" button + `isUploadModalOpen` modal — TO BE DELETED** (replaced by the `contribute` tab).

**Build order (agreed):** 1 route/tab-gating ✅ → 2 `fulltext_path` migration + `MsuirCsvImporter` service → 3 Flow C table → 4 Flow A CSV import → 5 Flow B wizard → 6 Flow D edit page → 7 Flow E review queue.

**Progress:**
- **Step 1 DONE (2026-09-02).** `/dashboard` back to `role:2`; `DashboardController@index` gates `members`/`departments` on `role_level >= 3` (smoke-tested: role 3 → 1 member + 49 deps, role 2 → 0/0); `Dashboard.vue` has `roleLevel`/`isAdmin` computeds, `activeTab` guard watch, `v-if="isAdmin"` on both members nav buttons. Build passes.
- **Step 2 DONE (2026-09-02).** Migration `2026_09_02_000001_add_fulltext_path_to_items_table` run (`fulltext_path` string nullable after `fulltext_url`). `Item` model: `fulltext_path` fillable + `fulltext` Attribute accessor (`fulltext_url ?: Storage::disk('public')->url(fulltext_path)`). `php artisan storage:link` done. New `App\Services\MsuirCsvImporter` — `parse(path)` (BOM skip, header find, positional contributor/subject, digit-only year, in-row dedup, `resolveDept`) → `{header,missingColumns,unexpectedColumns,rows[]}`; `validate(rows)` adds `status` (`ready`/`duplicate`/`error`) + `issues[]`, dup = existing item same `collection_id`+`title` OR seen earlier in file → `{summary:{total,ready,duplicate,error},rows}`; `commit(rows,status='approved',ownerId=null)` inserts `ready` rows only in one transaction → `{created,skippedError,skippedDuplicate}`; `truncateAll()` for `--fresh`. `ImportMsuir` command is now a thin wrapper over the service (parse→[truncate]→validate→commit). Smoke-tested: parse 279 rows OK, re-run without `--fresh` → `created:0, duplicate:279`, DB unchanged (items=279/person=559/subject=919).
- **Step 3 DONE (2026-09-02).** Flow C — repository table now real data, role-aware. `DashboardController@index` gained `private repositoryPayload(Request,$isAdmin,$userId)`: `Item::with('collection','creators')`, staff (`! $isAdmin`) scoped to `where('owner_id',$userId)`, filters `repo_q` (title / `whereHas('people',name like)` / id when numeric) + `repo_collection` + `repo_status`, `orderByDesc('id')->paginate(15)->withQueryString()`. Payload `repository = { items[{id,collection,title,author,year,status,canEdit}], pagination{total,currentPage,lastPage,perPage}, filters{q,collection,status}, collections[{id,name}] }`. `canEdit` = `$isAdmin || status IN (pending,action_required)`. New `App\Http\Controllers\Admin\RepositoryController@destroy` (soft delete, `back()->with()`), route `DELETE admin/repository/items/{item}` name `admin.repository.items.destroy` under the existing `role:3` admin group. `Dashboard.vue`: `RepositoryPayload`/`RepoItem` interfaces, `repository` added to `defineProps`, `repoFilters` ref seeded from `props.repository.filters`, `applyRepoFilters(extra)` → `router.get(route('dashboard'), {repo_q,repo_collection,repo_status,...extra}, { only:['repository'], preserveState, preserveScroll, replace })`, debounced `q` watch (350ms) + immediate watch on collection/status, `repoGoToPage`, `repoStatusMeta` badge map, `handleDeleteItem` (Swal confirm → `router.delete(..., { only:['repository'] })`). Repository tab template fully rewritten: search + 2 ModernSelect filters + "นำเข้า CSV" (admin → `activeTab='import'`) / "เพิ่มรายการ" (→ `activeTab='contribute'`) buttons; table cols ID · คอลเลกชัน · ชื่อเรื่อง/ผู้แต่ง · ปี · สถานะ · ปฏิบัติการ (edit if `canEdit` — currently just switches to contribute tab, real wiring in step 6; delete if `isAdmin`); prev/next pager. Dead mock removed: `collectionFilter` ref, `filteredSubmissions` computed. Sidebar (desktop + mobile) gained **เพิ่มรายการ** (`fa-file-circle-plus`, all) and **นำเข้าข้อมูล .csv** (`fa-file-csv`, `v-if="isAdmin"`) nav buttons; repo count badge now `props.repository.pagination.total`. Header title spans added for `contribute` / `import`. Placeholder tab bodies for `import` + `contribute` ("อยู่ระหว่างพัฒนา") — replaced in steps 4/5. Smoke-tested: admin → 279 items/19 pages/13 collections, `q=การพัฒนา`→56, `collection=5`→9, `status=pending`→0; fake staff → 0 (owns nothing). Build passes (Dashboard.js 65.8→71.3 kB). **Note:** dashboard-overview / approvals / analytics tabs still read the mock `submissions` ref (their own later steps / out of scope).
- **Step 4 DONE (2026-09-02).** Flow A — CSV bulk import wizard (admin-only `import` tab). `HandleInertiaRequests::share` gained a `flash` shared prop (`success`, `import` — both session closures). New `App\Http\Controllers\Admin\RepositoryImportController`: `template()` → `response()->download(base_path('database/data/elementmsuir-template.csv'))`; `validateUpload(Request, MsuirCsvImporter)` → validates `file` (`file|max:5120` + manual `.csv` extension check → `ValidationException`), stores at `storage/app/imports/{uuid}.csv`, `parse()` (catches `RuntimeException` → flash `{ok:false,error}` + delete file) then `validate()`, flashes `import` = `{ok:true, uuid, filename, missingColumns, unexpectedColumns, summary{total,ready,duplicate,error}, preview:[{line,title,collection_id,creator,department_name,year,status,issues}] (first 30)}`; `commit(Request, MsuirCsvImporter)` → validates `uuid`, re-parse+validate+`commit(status:'approved')` the stored file, `Storage::delete` in `finally`, flashes `import` = `{ok:true, done:true, result:{created,skippedError,skippedDuplicate}}` + `success`. Routes under the `role:3` admin group: `GET admin/repository/import/template` · `POST admin/repository/import/validate` · `POST admin/repository/import/commit` (names `admin.repository.import.{template,validate,commit}`). Template file `database/data/elementmsuir-template.csv` (kept in a git-tracked dir — `storage/app/*` is gitignored) — UTF-8 **BOM** + the 29-col header (contributor ×8, subject ×5 repeated) + 2 example rows. `Dashboard.vue`: `ImportReport`/`ImportPreviewRow`/`ImportIssue` interfaces; state `importStep` (1 upload / 2 review / 3 done) · `importBusy` · `importFile` · `importReport` · `importResult` · `importFileInput` ref · `importStatusMeta` badge map; `onImportFilePicked` → `router.post(validate, {file}, { forceFormData:true, only:['flash'] })`; `submitImportCommit` → `router.post(commit, {uuid}, { only:['flash','repository'] })` (refreshes the table too); `resetImport`; a `watch(() => page.props.flash?.import)` drives the step transitions + toasts. `import` tab template: 3-step indicator; step 1 = template download card (plain `<a :href="route('admin.repository.import.template')">`) + hidden `<input type=file accept=.csv>` label; step 2 = filename, header-issue banner, 4 summary stat cards, 30-row preview table with per-row status badge + issue list, "นำเข้า N รายการ" (disabled when `ready===0`) / "เลือกไฟล์ใหม่"; step 3 = success panel + "ดูในตารางคลังข้อมูล" (→ `activeTab='repository'`) / "นำเข้าไฟล์อื่น". End-to-end tested via controller (validate → 1 ready/1 error + uuid + stored file; commit → created 1, skipped 1, temp file deleted; items 279→280→cleaned to 279). Build passes (Dashboard.js 71.3→80.2 kB).
- **Step 5 DONE (2026-09-02).** Flow B — "เพิ่มรายการ" 5-step wizard (`contribute` tab, staff + admin). `RepositoryController@store(Request)`: `role:2`, validates the nested payload (`collection_id` exists, `title`, `language` in tha/eng, `alt_titles[]`/`contributors[]`/`subjects[]` arrays, `year_issued` int, `department_id` exists nullable, `rights`/`degree`/`description` nullable, `fulltext_url` url `required_without:fulltext_file`, `fulltext_file` `mimes:pdf|max:51200` `required_without:fulltext_url`); uploaded PDF → `store('fulltext','public')` → `fulltext_path`; `DB::transaction` creates the `Item` (`owner_id = auth id`, `status = $isAdmin ? 'approved' : 'pending'`, `format='pdf'`) + `titles()/people()/subjects()->create()` for each non-blank row (`array_filter(..., 'filled')`, creator first `sort_order` 0). Route `POST admin/repository/items` name `admin.repository.items.store` in a **new `role:2` admin group** (added above the existing `role:3` group). `DashboardController@index` now **always** sends `departments` (faculty list isn't PII, the wizard needs it) — no longer admin-gated. `Dashboard.vue`: `wizStep`(1–5)/`wizBusy`/`wizDone`/`wizForm` (`blankWizForm()` factory) / `wizFileInput` ref; `wizCollectionName`/`wizDepartmentName` computeds for the review panel; `wizStepValid(step)` (1 → collection+title, 5 → url or file), `wizNext`/`wizBack`, `wizAddRow`/`wizRemoveRow` for the 3 repeatable lists, `onWizFilePicked`, `resetWizard`, `wizSubmit` → `router.post(route('admin.repository.items.store'), payload, { forceFormData:true, only:['flash','repository'], onSuccess → wizDone, onError → toast first error })`. `contribute` tab template: done-panel OR [5-dot step indicator + per-step panels (1 collection ModernSelect + title textarea + language toggle + alt-title rows; 2 creator + contributor rows; 3 year/department/rights/degree/description; 4 subject rows; 5 url|file radio + review summary + "สถานะเมื่อบันทึก" hint) + Back/Next/บันทึกรายการ footer]. Smoke-tested via controller: staff → `pending` + owner + 1 alt/1 creator/2 contrib/2 subj (blanks filtered), admin → `approved`; missing collection / missing fulltext / bad url all rejected; DB back to 279.
- **Icon + tab-persistence fixes (2026-09-02):** FA CDN is **6.0.0** — `fa-file-circle-plus` (6.1+) rendered blank, swapped the "เพิ่มรายการ" nav icon (desktop + mobile) to `fa-circle-plus`. `activeTab` now seeds from + writes to `sessionStorage['msuir.dashboard.activeTab']` (validated against `validTabs`) so a `router.post`→`back()` round-trip (CSV upload) no longer knocks the tab back to `dashboard` when the component re-inits; the `flash.import` watcher got `{ immediate: true }` so it still catches a report present on a fresh mount.
- **Steps 3+4 tabs + Flow E + overview/analytics DONE (2026-09-02).** Migration `2026_09_02_000002_add_review_note_to_items_table` (`review_note` text nullable after `status`) run; `Item` `review_note` fillable. `DashboardController@index` now also sends `stats` + `queue`:
  - `statsPayload($isAdmin,$userId)` — role-aware scope (`$scope = fn() => Item::query()->when(!$isAdmin, where owner_id)`); returns `total/approved/pending/actionRequired`, `byCollection[{name,count}]` (sorted desc, count>0), `byLanguage{tha,eng}`, `byYear[{year,count}]` (last 8), `topFaculties[{name,count}]` (top 6 by `department_id`), `recent[{id,title,collection,author,status}]` (latest 6).
  - `queuePayload($isAdmin,$userId)` — admin → `whereIn status [pending,action_required]`; staff → `where owner_id` (all statuses); each `{id,title,collection,author,year,status,reviewNote,owner (admin only),submittedAt}`, limit 100.
  `RepositoryController` gained `approve(Item)` (→ approved, `review_note`=null) and `returnForEdit(Request,Item)` (validates `note` required, → action_required + note); routes `PATCH admin/repository/items/{item}/approve|return` in the `role:3` group (names `admin.repository.items.approve` / `.return`).
  `Dashboard.vue`: removed the mock `Submission` interface + `submissions` ref + `totalItems`/`approvedItems`/`pendingItems`/`actionRequiredItems` computeds + mock `handleApprove`/`handleReject`/`handleDelete` + `NewDoc`/`newDoc`/`handleCreateDocument` + the header "เพิ่มผลงานใหม่" button + the whole upload modal + its `isUploadModalOpen` watch. Added `ItemStatus` type, `DashboardStats`/`QueueItem` interfaces, `stats`/`queue` props, shared `statusMeta` badge map (replaces `repoStatusMeta`), `queueBadgeCount` computed (sidebar badge — admin: `queue.items.length`; staff: own `action_required` count), `max{Collection,Year,Faculty}Count` computeds for bar scaling, `handleQueueApprove` / `handleQueueReturn` (Swal textarea prompt → `router.patch(..., { only:['queue','stats','repository'] })`). Overview tab rewritten: 4 real stat cards + "แยกตามคอลเลกชัน"/"แยกตามปี" bar lists + real recent table (read-only). Approvals tab → Flow E queue cards (admin: approve / ส่งกลับแก้ไข; staff: read-only + review-note banner + "แก้ไข" → contribute tab). Analytics tab → real aggregates (total / language split / pending, byCollection + topFaculties bars) with a "no view/download tracking" disclaimer. `contribute` tab lost its `max-w-3xl` (now full width). Icon fix carried: FA 6.0.0 so nav "เพิ่มรายการ" uses `fa-circle-plus`. Smoke-tested Flow E: pending item shows in admin queue + `stats.pending`; returnForEdit → action_required+note; approve → approved+note cleared; staff sees own items, `owner` null for staff. Build passes (Dashboard.js 97→85.6 kB).
- **Step 6 DONE (2026-09-02) — repository-management plan COMPLETE.** Flow D — single flat edit page. `RepositoryController` refactored: shared `rules(bool $fulltextRequired)` + `syncChildren(Item,$data)` (delete-all + recreate) + `authorizeItemEdit(Request,Item)` (`abort_unless($isAdmin || ($item->owner_id === user id && status IN [pending,action_required]), 403)`). New `edit(Request,Item): Response` → `Inertia::render('Repository/ItemEdit', { item{…flattened incl. alt_titles/creator/contributors/subjects/review_note}, collections[{id,name}], departments[{id,name}] })`. New `update(Request,Item)` → validates (`fulltextRequired` only when neither url nor stored path exists), swaps the uploaded PDF (deletes the old file; a pasted URL also supersedes+deletes a stored file), `DB::transaction` updates metadata + `titles()/people()/subjects()->delete()` then `syncChildren`; **a non-admin editing an `action_required` item flips it back to `pending` and clears `review_note`** (resubmit); redirects to `dashboard` with `success`. Routes `GET admin/repository/items/{item}/edit` + `PUT admin/repository/items/{item}` (names `admin.repository.items.edit`/`.update`) in the **`role:2`** group. New page `resources/js/pages/Repository/ItemEdit.vue` — own chrome (FA `<link>` in `<Head>`, Anuphan font, back-to-dashboard link, status badge, review-note banner), 5 stacked `<section>` cards (ประเภท/ชื่อเรื่อง · ผู้แต่ง · บรรณานุกรม · หัวเรื่อง · ไฟล์) with the same repeatable-row pattern as the wizard, submit via `router.post(route('admin.repository.items.update', id), { _method:'put', … }, { forceFormData:true })`, inline `errors` from `onError`. Flow C table edit button + Flow E staff "แก้ไข" button are now `<Link :href="route('admin.repository.items.edit', item.id)">` (were `activeTab='contribute'` stopgaps). Smoke-tested: edit payload flattens children + 13 collections; staff update rebuilds all child rows + `action_required`→`pending`+note cleared; authz blocks a non-owner (403) and an owner on an already-`approved` item (403). Build passes (ItemEdit chunk 14 kB; Dashboard.js 85.6→84 kB).

**Repository-management plan (all 7 steps) is done.**

## Public pages → real data (2026-09-02)

- **Item page `/item/{id}` — DONE.** `ItemController@show(int $id)`: `Item::with('collection','department','titles','people','subjects')->where('status','approved')->findOrFail($id)` (non-approved → 404), related = same collection, 4 latest approved, exclude self. Payload `item { id, title, title_en (first alt title), authors (creators names[]), contributors (names[]), faculty (dept name), year, type (collection name), language ('ภาษาไทย'|'English'), abstract (description), keywords (subjects[]), rights, fulltext_url (the `fulltext` accessor), collection{id,name} }` + `relatedItems[{id,title,author,year,type}]`. `Item.vue` `<script setup>` de-mocked (props required, no `withDefaults`): dropped `FileAttachment`/`advisors`/`department`/`doi`/`abstract_en`/`views`/`downloads`/`files[]`/`typeStyle`/`fileIcon`/`fileColor`/`isBookmarked`/`showFullAbstractEn`. Tabs are now **abstract / file / citation** (`file` = single fulltext link card, not a files list); removed the stats bar, bookmark+share buttons, DOI blocks, EN-abstract block, EndNote/Zotero/Print export row; download/อ่านออนไลน์ → one `<a :href="item.fulltext_url" target="_blank">` (or a "ยังไม่มีไฟล์" chip); metadata table advisors→"ผู้แต่งร่วม" (contributors), keywords→"หัวเรื่อง" (subjects), added rights row; APA/BibTeX citations rebuilt generic (`@misc`, no DOI). Smoke-tested (`/item/2`): real fields + 4 related + 404 for missing id.
- **Collection page `/collection/{id}` — was already real; added `->where('status','approved')` to the items query + both facet queries** so the public page never shows pending/action_required items.
- **Home `/` — DONE.** Route `/` now `[HomeController::class, 'index']` (was an inline `Inertia::render('Welcome')` closure). `HomeController@index`: `collections` = all 13 with `withCount('items' where approved)` → `{id,name,name_en,count}`; `$latest` = 7 latest approved with collection/creators/department; `recommended` = `$latest->take(2)`, `newReleases` = `$latest->slice(2,5)`; `stats` = `{ total (approved count), byCategory [{name (category name_en), count}] via items⋈collections⋈categories group-by }`. `Welcome.vue` de-mocked → props `{collections, recommended, newReleases, stats}`; category cards show real name + `count` รายการ; Recommended/New Release lists wired (New Release shows collection·faculty·year instead of the fake "2 ชั่วโมงที่ผ่านมา"); "Discover" widget shows `stats.byCategory`; the hero "สืบค้น" box now `submitSearch()` → `router.get(route('collection.show', <largest collection id>), { q })` (interim — no global search route yet, that's part of the analytics/`search_logs` plan).
- **PublicNavbar — DONE.** `HandleInertiaRequests::share` gained a `publicNav` shared prop (closure): `Category::with('collections' orderBy sort_order)->orderBy(sort_order)` → `[{ title: category name_en, links: [{name: collection name_th|name_en, href: "/collection/{id}"}] }]`. `PublicNavbar.vue` replaced its hard-coded `collectionGroups` array with `computed(() => page.props.publicNav ?? [])`.

**`Collection.vue` tweaks (2026-09-02):** year filter shows the 5 most recent years by default (`visibleYears` computed, `YEAR_PREVIEW = 5`) with an "แสดงอีก N ปี" / "ย่อ" toggle (`isYearExpanded`). `CollectionController@show` pagination is now **`paginate(10)`** (was 20).

**Dashboard status recap (for the user's repeated question):** overview tab = fully real; analytics tab = real but **count-based only** — there is no view/download/search tracking anywhere in the schema, so those panels were removed and replaced with a disclaimer.

**Next up: the view/download/search statistics system** (agreed sequencing) — `item_events` table + counters + a download-proxy route (`GET /item/{id}/download` logs then redirects) + view logging in `ItemController@show` + `search_logs` (log queries in `CollectionController@show`) + rebuild the dashboard analytics tab on real events. PDPA: hash/session tokens, no raw IP.

**NOTHING is committed to git yet.** Everything from the repository-management plan + public-pages wiring is uncommitted working-tree changes. Commit when the user asks.

**Older TODO:**
1. **Single item page** `/item/{id}` — `ItemController@show` + `Item.vue` still need wiring to real data (separate from the repository-management work).
2. Nothing committed to git yet. Commit when the user asks.
